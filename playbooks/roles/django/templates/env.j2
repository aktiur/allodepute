# {{ ansible_managed }}

# make sure debug mode is not enabled
DEBUG=False

# security parameters
SECRET="{{ django_secret }}"
ALLOWED_HOSTS="{{ django_domain }}"

STATIC_ROOT="{{ django_static_directory }}"
MEDIA_ROOT="{{ django_media_directory }}"

{% for env, value in django_env.items() %}
{{ env }}={{ value }}
{% endfor %}
